<section class="card">
  <a href="/painting/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{capName artist}}</p>
      {{!-- capName(artist) --}}
    </div>
  </a>
</section>