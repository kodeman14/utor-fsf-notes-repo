//WRITE YOUR CODE BELOW
var studentsArr = ["Sarah", "Orlando", "Heather", "Ismael", "Imon"];

console.log(studentsArr.length);

//* It's done when, using a `for` loop, the greeting "Great to see you, CLASSMATE_NAME!" logs to the console for each classmate's name in the `students` array.

for (var i = 0; i < studentsArr.length; i++) {
  console.log("great to see you i guess " + studentsArr[i]);
}

// for (initial step; condition until when to run; increment/decrement count)
